---
// src/components/Button.astro
// Props for dynamic behavior and styling
import '../styles/Button.css';
const { label, onClick, className = 'primary' } = Astro.props;
---

<button class={`btn ${className}`} onclick={onClick}>{label}</button>