---
import { getCollection } from 'astro:content';
import SlideshowComponent from '../components/Slideshow.jsx';

const { type } = Astro.props;

const slides = await getCollection(
  'slides',
  (slide) => slide.slug.startsWith(`${type}/`)
);
---

<SlideshowComponent slides={slides} client:idle />